name: aws-resolver-rules-operator
serviceType: managed

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"

image:
  registry: quay.io
  name: giantswarm/aws-resolver-rules-operator
  tag: v0.0.1
  pullPolicy: IfNotPresent
